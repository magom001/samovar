{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "prettier-check": "prettier \"**/*.{ts,tsx,md}\" --check",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "prettier": "^3.0.3",
    "tsconfig": "*",
    "turbo": "latest",
    "typescript": "^5.3.2"
  },
  "name": "samovar",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}